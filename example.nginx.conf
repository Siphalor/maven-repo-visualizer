server {
	# when mapping this assets location, set MAVENRV_ASSETS_PATH to /mavenrc/assets/
	location ^~/mavenrc/assets/ {
		alias /<path to repo>/src/assets/;

		location ~.svg$ {
			add_header Cache-Control "max-age=864000";
		}
		location ~.css$ {
			add_header Cache-Control "max-age=7200";
		}
	}

	location ~/$ {
		include fastcgi_params;
		fastcgi_pass unix:run/php/php-fpm.sock;
		fastcgi_param SCRIPT_FILENAME /<path to repo>/src/auto_index.php;
		fastcgi_param DOCUMENT_ROOT /var/www/maven;
	}

	location / {
		root /var/www/maven;
	}
}